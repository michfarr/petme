<%= form_for(@user) do |f| %>
  <% if @user.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@user.errors.count, "error") %> prohibited your name from being saved:</h2>

      <ul>
      <% @user.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name, class: "form-controll", placeholder: " Enter your username" %>
  </div>
  <div class="field">
    <%= f.label :email %><br>
    <%= f.text_field :email, class: "form-controll", placeholder: " Enter your email" %>
  </div>
  <div class="field">
    <%= f.label :profile_picture %><br>
    <%= f.text_field :profile_picture, class: "form-controll", placeholder: " Enter image URL" %>
  </div>

  <div class="actions">
    <%= f.submit "Continue", class: "btn btn-default" %>
  </div>
<% end %>
